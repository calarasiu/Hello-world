<%= simple_form_for([post, Comment.new], remote: true)do |f| %>
  <%= f.input :content, label: false, placeholder: "Write your comment" %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>

